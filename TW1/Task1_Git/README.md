# Task 1 - Git Workflow & Collaboration

## Objective

Initialize a Git repository for the Flask Hello World application, create a feature branch, make a change, create and resolve a merge conflict, and push the resulting work to GitHub.

## Recovered Git History

The original backup contains the repository's `.git` history with these relevant commits:

```text
8f3d93c  Initial commit - Flask Hello World application
67f28c7  Add user authentication feature change
1061cd4  Update homepage greeting in main
67c3fc3  Merge branch 'feature/user-auth' into main - resolved conflicts
```

## Source

The `source/` directory contains the Flask application files used for the Git task.

## Evidence

The `screenshots/` directory contains the original saved screenshots for local Flask verification, initial commit, GitHub main branch, feature branch work, feature branch on GitHub, and merge conflict.

The final conflict-resolution state is also preserved in the recovered Git history.
