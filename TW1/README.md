# TW1 - Continuous Evaluation: Foundational DevOps Skills

This directory contains the recovered and organized work for TW1, grouped into the three assessment tasks.

## Task 1 - Git Workflow & Collaboration

The original Flask Hello World repository and its Git history were preserved in the backup. The recovered workflow includes the `main` branch, the `feature/user-auth` branch, the feature commit, the conflicting change on `main`, and the final merge commit resolving the conflict.

Evidence screenshots are stored in `Task1_Git/screenshots/`.

## Task 2 - Jira Project & Issue Tracking

The recovered Jira evidence shows the `DevOps Lab - TW1` project backlog with the required Story, Task, and Bug. The `Setup Flask Environment` task is shown in `In Progress`.

Evidence is stored in `Task2_Jira/screenshots/`.

## Task 3 - Docker & Jenkins Freestyle Project

The recovered Docker evidence documents the Dockerfile, image build, running container, and Flask browser verification on port 5000.

The Jenkins evidence documents the Freestyle project setup, a successful build, and the successful build console output showing the repository checkout and workspace listing.

Evidence is stored in `Task3_Docker_Jenkins/screenshots/`.

## Structure

```text
TW1/
├── README.md
├── Task1_Git/
│   ├── README.md
│   ├── source/
│   └── screenshots/
├── Task2_Jira/
│   ├── README.md
│   └── screenshots/
└── Task3_Docker_Jenkins/
    ├── README.md
    ├── source/
    └── screenshots/
```
