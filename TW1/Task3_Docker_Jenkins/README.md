# Task 3 - Docker & Jenkins Freestyle Project

## Objective

Containerize the Flask Hello World application with Docker and configure a Jenkins Freestyle project to pull the Git repository and run a workspace-listing build step.

## Docker

The recovered evidence shows:

- Dockerfile configuration
- Successful Docker image build
- Running container with port 5000 exposed
- Flask application verified in the browser

The original Flask source and Dockerfile are included in `source/`.

## Jenkins

The recovered evidence shows:

- Jenkins Freestyle project setup
- Successful `DevOps-TW1-Flask` build #2
- Git repository checkout
- Workspace contents listed
- `Finished: SUCCESS`

## Evidence

The screenshots in this directory are ordered from Docker setup through successful Jenkins execution.
